import React from 'react';
import ReactDOM from 'react-dom';
import PaperList from './PaperList'; // import your main component
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
